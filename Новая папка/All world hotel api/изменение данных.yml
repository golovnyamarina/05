info:
  name: изменение данных
  type: http
  seq: 4

http:
  method: PATCH
  url: http://127.0.0.1:8000/api/bookings/3
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
