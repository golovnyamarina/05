info:
  name: добавление тура
  type: http
  seq: 7

http:
  method: POST
  url: http://127.0.0.1:8000/api/tours/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
